# Original dsh reference source (kept for design reference, not tracked)
reference/

# Original opencode skills (from upstream dsh, not used by lite)
.opencode/

# Design documents (local only, not tracked)
DESIGN-lite.md
DESIGN-UNIFIED.md

# Rust build artifacts
target/
**/*.rs.bk
*.pdb

# Release binaries (built per phase, not tracked in git)
release/

# IDE / OS
.vscode/
.idea/
.DS_Store

# Local config / env
.env
*.local

# Session / runtime data
.sessions/
.storages/
.cache/

# Test artifacts
.test-memory-*
.test-sessions-*

# Explore / design notes (working docs, not shipped)
.explore-notes.md
