# DSH 上游 v0.1.3-alpha.2 增量分析报告

> 对比范围：`dsh-v0.1.3-alpha.1` → `dsh-v0.1.3-alpha.2`（master `d347e70390` → `c389f96bf3`）
> 非合并提交：236 个，285 文件变更（+11251 / -4903）
> 稳定 RC 目标：**v0.1.2-rc.1 未变**（上次已同步完，本次无新稳定版本需迁移）
> 性质：alpha 预览增量，破坏性 v0.1.3 未到稳定阶段

## 一、结论速览

上游新增 `dsh-v0.1.3-alpha.2`，但**没有新的稳定 RC**。236 个提交中绝大多数对 Lite 不适用：

| 类别 | 提交数 | 对 Lite |
|------|--------|---------|
| subprocess / native runner | 112 | ❌ Lite 无 PTY/node-pty |
| perf / benchmark | 40 | ❌ 上游 CI 基准 |
| docs / notes / i18n | 17 | ❌ 文档 |
| ci / release | 13 | ❌ 构建流程 |
| session-format v2 | 5 | ❌ 破坏性 alpha（此前已决定不同步） |
| 其他（约 49） | 49 | 逐项筛出下述可跟进项 |

## 二、✅ 应该跟进（无需讨论）

### 1. system-prompt 环境事实后置（cwd 移到 suffix）

- **上游变更**：`40792330c0` + `e28862db57`。上游把 persona 拆成 `persona-prefix`（order 0，保留模型名介绍）+ `persona-suffix`（order 10200，放 cwd / Web URL / checkout path 等环境事实）。动机：环境事实因用户/机器而异，放在 prompt 前部会让可复用前缀过早分叉，破坏跨会话的 KV cache 复用。
- **Lite 现状**：`src/prompt.rs` 的 identity 段（order=-100，最前）是 `"You are an AI agent. Working directory: {{cwd}}."`——**cwd 恰好放在了最前面**，与 Lite 自己在 prompt.rs 注释里声明的"fixed content at head / dynamic at tail"目标直接矛盾。cwd 是动态环境事实，却被固化在 order=-100。
- **影响**：不跟则 Lite 声称的 KV cache 前缀优化实际失效——cwd 一变，整个 system prompt 前缀就变，缓存命中率下降。
- **工作量**：S。把 cwd 从 identity 段拆出，单独成段（order 放到 custom-prompt 之后，如 order=20），identity 只保留固定的 "You are an AI agent."。

## 三、🔍 需要讨论

### 2. 模型切换公告（announce model switches）

- **上游做了什么**：`48cc1cf1d6`。当会话中途切换模型时，向新模型插入一条 user-role 消息：`[model changed: assistant turns above this point were generated by <previous>; the session continues with <next>]`，避免新模型看到旧 turn 的图片占位符后误判自己能力。
- **Lite 现状**：Lite 有模型 hot-switch（preset 下拉），但切换后新模型**静默接手**旧上下文，无任何提示。
- **跟进的利**：模型切换后语义更清晰，避免多模态模型误读历史。
- **跟进的弊**：多一条 user 消息占用 context token；Lite 场景多为单设备、单模型，切换频率低。
- **我的建议**：**跳过**。Lite 的 vision 能力尚未接入（#25 之前已决定跳过），公告的价值场景不存在。

### 3. SSE 断线自动重连（continuous client recovery）

- **上游做了什么**：`1bd26370cc` + `1e04fcff35`。Web 客户端断线后自动指数退避重连（封顶），offline/online 事件联动。基于 WebSocket mux + ConnectionController。
- **Lite 现状**：Lite 前端是 `fetch` + `reader` 的一次性 SSE（POST /api/chat），断线即结束，无重连。上轮 #21 已加了连接状态指示器（就绪/连接中/接收中/错误），但**无自动重连**。
- **跟进的利**：网络抖动时用户不用重新发消息。
- **跟进的弊**：Lite 的 SSE 是"一个 turn 一个 POST"，断线后要"续传"得回到服务端拿历史，改动涉及前后端（工作量 M）；且 Lite 面向设备内网直连，断线场景少。
- **我的建议**：**跳过**。Lite 架构与上游的 WebSocket mux 差异大，重连语义不直接映射；#21 的状态指示已覆盖基本体验。

### 4. resume 去重 + 系列边界

- **上游做了什么**：`6ad01cbd8d` + `477ff9d5e3`。修复 resume 时系统提示词重复注入、保留 resumed series 的 `startsSeries` 边界标记。
- **Lite 现状**：Lite 的 system prompt 是每次 `run_turn` 用 `prompt::assemble` 现算的，不存进 session 事件；session resume 走 checkpoint 重放 + `derive_messages`。架构不同，不存在"系统提示词重复"的同类 bug。
- **跟进的利/弊**：上游修的是它自己"系统提示词存进 session 事件"的架构问题，Lite 没有这个结构。
- **我的建议**：**跳过**。不适用（架构差异）。

## 四、⏸️ 暂不跟进 / ❌ 不适用（简略）

- **subprocess / native runner（112 提交）**：Lite 无 PTY，用 `std::process::Command` 直接拿 stdout+stderr+exit code，已覆盖 `2660635e63`（shell 保留 stderr）等语义。
- **perf/bench（40）、docs（17）、ci/release（13）**：上游 CI 基准与文档。
- **session-format v2（5）**：破坏性格式迁移，此前已决定 alpha 预览不同步，等稳定版。
- **str_replace_editor 默认禁用**（`36a4665144` + `965adbb5cf`）：Lite 无此工具（用 file_write），不适用。
- **subagent 消息前缀空格**（`754a65ab2d`）：Lite 无双向 send_message（#26 此前跳过），不适用。
- **workspace files / Sidebar / dockkit（web-ui 系列）**：Lite 自有前端，不映射。

## 五、总结

| # | 变更 | 分类 | 工作量 | 建议 |
|---|------|------|--------|------|
| 1 | cwd 从 identity 拆到 suffix | ✅ 跟进 | S | 立即做 |
| 2 | 模型切换公告 | 🔍 讨论 | S | 跳过（vision 未接入） |
| 3 | SSE 断线自动重连 | 🔍 讨论 | M | 跳过（架构差异大） |
| 4 | resume 去重 + 系列边界 | 🔍 讨论 | S | 跳过（架构差异） |

**核心结论**：无新稳定 RC，仅 alpha 增量。唯一值得跟进的是 **#1（cwd 后置）**，一个小改动修正 Lite 自身的 KV cache 前缀矛盾。
